{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build SerendipityBooksellers",
            "type": "shell",
            "command": "g++ -fdiagnostics-color=always -g ${workspaceFolder}/part7/*.cpp -o ${workspaceFolder}/part7/mainmenu",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}
