{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build SerendipityBooksellers",
            "type": "shell",
            "command": "g++ -fdiagnostics-color=always -g ${workspaceFolder}/part8/*.cpp -o ${workspaceFolder}/part8/mainmenu",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}
